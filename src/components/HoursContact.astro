---
import infos from '../data/infos.json';
import PhoneIcon from './PhoneIcon.astro';
const adr = infos.adresse;
const adresseTexte = `${adr.rue}, ${adr.ville}, ${adr.province} ${adr.codePostal}`;
const mapQuery = encodeURIComponent(`${infos.nom}, ${adresseTexte}`);
const livraisonActive = infos.livraison.filter((l) => l.midi || l.soir);
---
<section id="heures" class="scroll-mt-20 bg-cream py-14">
  <div class="mx-auto grid max-w-5xl gap-8 px-4 md:grid-cols-2">
    <div>
      <p class="text-xs font-semibold tracking-[0.2em] text-brick">HEURES &amp; CONTACT</p>
      <h2 class="mt-2 font-display text-3xl font-bold">Venez nous voir</h2>

      <h3 class="mt-6 font-display text-xl font-bold">Heures d'ouverture</h3>
      <table class="mt-2 w-full text-sm sm:text-base">
        <tbody>
          {infos.heures.map((h) => (
            <tr class="border-t border-cream-line">
              <th scope="row" class="py-2 text-left font-semibold">{h.jour}</th>
              <td class="py-2 text-right tabular-nums">{h.ouvert ? `${h.ouvert} – ${h.ferme}` : 'Fermé'}</td>
            </tr>
          ))}
        </tbody>
      </table>

      <h3 class="mt-8 font-display text-xl font-bold">Heures de livraison</h3>
      <table class="mt-2 w-full text-sm sm:text-base">
        <tbody>
          {livraisonActive.map((l) => (
            <tr class="border-t border-cream-line">
              <th scope="row" class="py-2 text-left font-semibold">{l.jour}</th>
              <td class="py-2 text-right">{[l.midi, l.soir].filter(Boolean).join(' · ')}</td>
            </tr>
          ))}
        </tbody>
      </table>

      <div class="mt-3 flex flex-wrap items-center gap-x-3 gap-y-2">
        <span class="flex items-center gap-1.5" role="img" aria-label="Interac, Visa et Mastercard acceptés">
          <span aria-hidden="true" class="grid h-7 place-items-center rounded border border-cream-line bg-white px-2 text-xs font-bold text-charcoal">Interac</span>
          <span aria-hidden="true" class="grid h-7 place-items-center rounded border border-cream-line bg-white px-2 text-[13px] font-extrabold italic tracking-tighter" style="color:#1A1F71">VISA</span>
          <span aria-hidden="true" class="grid h-7 place-items-center rounded border border-cream-line bg-white px-2">
            <svg viewBox="0 0 30 19" class="h-4 w-auto">
              <circle cx="11" cy="9.5" r="8" fill="#EB001B" />
              <circle cx="19" cy="9.5" r="8" fill="#F79E1B" />
              <path d="M15 2.57a8 8 0 0 1 0 13.86 8 8 0 0 1 0-13.86Z" fill="#FF5F00" />
            </svg>
          </span>
        </span>
        <p class="text-sm text-charcoal/65">Des frais de livraison s'appliquent.</p>
      </div>

      <address class="mt-6 not-italic">
        <p class="font-semibold">{infos.nom}</p>
        <p class="text-charcoal/70">{adresseTexte}</p>
        <a href={`tel:${infos.telephoneLien}`} class="mt-2 inline-flex items-center gap-1.5 font-semibold text-brick"><PhoneIcon class="h-4 w-4" /> {infos.telephoneAffiche}</a>
      </address>
      <a href={`https://www.google.com/maps/search/?api=1&query=${mapQuery}`} target="_blank" rel="noopener noreferrer" class="mt-4 inline-block rounded-lg bg-brick px-4 py-2.5 font-semibold text-white transition hover:bg-brick-dark">
        Obtenir l'itinéraire
      </a>
    </div>

    <div class="min-h-[320px] overflow-hidden rounded-xl shadow-sm">
      <iframe
        title="Carte — Restaurant 4 Saisons"
        class="h-full min-h-[320px] w-full border-0"
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        src={`https://www.google.com/maps?q=${mapQuery}&output=embed`}
      ></iframe>
    </div>
  </div>
</section>
