---
import PriceTable from './PriceTable.astro';
import Carousel from './Carousel.astro';
const { category, images = [], open = false } = Astro.props;
const count = category.items.length;
---
<details open={open} class="menu-cat group overflow-hidden rounded-xl bg-white shadow-sm transition hover:shadow-md">
  <summary class="flex cursor-pointer select-none items-center justify-between gap-3 px-4 py-4 sm:px-5">
    <span class="flex flex-wrap items-center gap-x-2 gap-y-0.5 font-display text-lg font-bold text-charcoal">
      <span>
        {category.nom}
        {count > 0 && <span class="ml-1.5 text-sm font-normal text-charcoal/60">({count})</span>}
      </span>
    </span>
    <svg class="chevron h-5 w-5 shrink-0 text-brick" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <path d="m6 9 6 6 6-6" />
    </svg>
  </summary>

  <div class="border-t border-cream-line px-4 pb-4 pt-3 sm:px-5">
    {images.length > 0 && (
      <div class="mb-4">
        <Carousel images={images} label={`Plats — ${category.nom}`} />
      </div>
    )}
    {category.note && (
      <p class="mx-auto mb-4 w-fit max-w-full rounded-full bg-brick/10 px-4 py-1.5 text-center text-sm font-medium text-brick-dark sm:text-base">
        {category.note}
      </p>
    )}
    {category.columns ? (
      <PriceTable category={category} />
    ) : category.items.length === 0 ? null : (
      <ul class="divide-y divide-cream-line">
        {category.items.map((item) => (
          <li class="flex items-baseline justify-between gap-4 py-2.5">
            <span class="font-semibold">
              {item.nom}
              {item.desc && <span class="block text-xs font-normal text-charcoal/65">{item.desc}</span>}
            </span>
            <span class="tabular-nums font-semibold whitespace-nowrap">{item.price} $</span>
          </li>
        ))}
      </ul>
    )}
  </div>
</details>
